# Minimal gateway package — restored utility modules only.
# The full messaging gateway (platforms, relay, run, sessions) was removed:
# this is plumbing (session context, status helpers, config, platform
# registry) that the agent core / TUI still import.
